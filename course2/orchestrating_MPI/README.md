# Orchestrating MPI applications with the ProcessGroup API
DragonHPC’s ProcessGroup API allows users to
run Python functions, serial executables, and parallel applications (e.g., MPI-based) as a key ingredient for many
HPC workflows. The goal for this session is to show participants of all levels how to orchestrate MPI applications,
including several at a time, while managing placement and I/O.
